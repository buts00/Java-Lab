package Players;
/**
 * Represents a bot player in the game with a specified health.
 */
public class Bot extends PlayerBase {
    /**
     * Constructs a bot player with the given health.
     * @param health The initial health of the bot player.
     */
    public Bot(int health) {
        super(health);
    }
}