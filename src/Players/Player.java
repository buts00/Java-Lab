package Players;

/**
 * Represents a player in the game with a specified health.
 */
public class Player extends PlayerBase {
    /**
     * Constructs a player with the given health.
     * @param health The initial health of the player.
     */
    public Player(int health) {
        super(health);
    }
}